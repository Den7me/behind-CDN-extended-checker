name: Docker Build & Push (disabled)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Docker build disabled by design
        run: |
          echo "Docker auto-build and auto-push are intentionally disabled."
          echo "Images are built and pushed manually by the maintainer."

